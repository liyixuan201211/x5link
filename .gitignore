# ── 运行产物，不该入库 ─────────────────────────────────────────────
__pycache__/
*.py[cod]
x5link-app.log

# ffmpeg 的 stderr 日志（web.py 每次重拉都覆盖写）
x5link-ffmpeg.log

# ── macOS 目录元数据 ────────────────────────────────────────────────
.DS_Store
._*

# ── 录制 / 素材输出（体积大，可重建）───────────────────────────────
*.mov
*.mp4
recordings/
out/
